# --- ESP-IDF build output ---
build/

# --- ESP-IDF component manager ---
# Downloaded components (lvgl, sh8601, button, …) — regenerated by `idf.py reconfigure`.
managed_components/
# dependencies.lock is intentionally TRACKED (pins component versions for reproducible builds).

# --- ESP-IDF project config (machine-specific) ---
sdkconfig
sdkconfig.old

# --- IDE / tooling caches ---
.cache/
.vscode/

# --- Local reference material (SDK docs/examples — not part of the firmware build) ---
resources/

# --- Python ---
__pycache__/
*.pyc

# --- macOS ---
.DS_Store
